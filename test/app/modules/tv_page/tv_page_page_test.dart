import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('TvPagePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TvPagePage(title: 'TvPage')));
    //  final titleFinder = find.text('TvPage');
    //  expect(titleFinder, findsOneWidget);
  });
}
