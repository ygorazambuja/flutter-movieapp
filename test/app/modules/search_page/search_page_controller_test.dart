import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:bshare/app/app_module.dart';

void main() {
  initModule(AppModule());
  // SearchPageController searchpage;
  //
  setUp(() {
    //     searchpage = AppModule.to.get<SearchPageController>();
  });

  group('SearchPageController Test', () {
    //   test("First Test", () {
    //     expect(searchpage, isInstanceOf<SearchPageController>());
    //   });

    //   test("Set Value", () {
    //     expect(searchpage.value, equals(0));
    //     searchpage.increment();
    //     expect(searchpage.value, equals(1));
    //   });
  });
}
