import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DrawerPagePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DrawerPagePage(title: 'DrawerPage')));
    //  final titleFinder = find.text('DrawerPage');
    //  expect(titleFinder, findsOneWidget);
  });
}
