import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:bshare/app/app_module.dart';

void main() {
  initModule(AppModule());
  // FilmPageController filmpage;
  //
  setUp(() {
    //     filmpage = AppModule.to.get<FilmPageController>();
  });

  group('FilmPageController Test', () {
    //   test("First Test", () {
    //     expect(filmpage, isInstanceOf<FilmPageController>());
    //   });

    //   test("Set Value", () {
    //     expect(filmpage.value, equals(0));
    //     filmpage.increment();
    //     expect(filmpage.value, equals(1));
    //   });
  });
}
