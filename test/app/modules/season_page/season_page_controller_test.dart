import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:yshare/app/app_module.dart';

void main() {
  initModule(AppModule());
  // SeasonPageController seasonpage;
  //
  setUp(() {
    //     seasonpage = AppModule.to.get<SeasonPageController>();
  });

  group('SeasonPageController Test', () {
    //   test("First Test", () {
    //     expect(seasonpage, isInstanceOf<SeasonPageController>());
    //   });

    //   test("Set Value", () {
    //     expect(seasonpage.value, equals(0));
    //     seasonpage.increment();
    //     expect(seasonpage.value, equals(1));
    //   });
  });
}
