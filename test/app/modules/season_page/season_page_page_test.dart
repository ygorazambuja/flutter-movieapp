import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SeasonPagePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SeasonPagePage(title: 'SeasonPage')));
    //  final titleFinder = find.text('SeasonPage');
    //  expect(titleFinder, findsOneWidget);
  });
}
